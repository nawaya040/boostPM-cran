% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/postprocessing.R
\name{eval_density_b}
\alias{eval_density_b}
\title{Evaluate a Fitted boostPM Log Density}
\usage{
eval_density_b(list_boosting, eval_points)
}
\arguments{
\item{list_boosting}{A fitted object returned by \code{\link[=boosting]{boosting()}}.}

\item{eval_points}{A finite numeric matrix with evaluation points in rows
and one column per fitted variable.}
}
\value{
A list with two components: \code{log_densities}, the log density for
each row of \code{eval_points}; and \code{mean_log_dens_path}, the cumulative mean
log-density path after each fitted tree.
}
\description{
Evaluates the log density represented by a fitted tree ensemble at supplied
points.
}
\details{
Points outside the fitted rectangular support receive log density \code{-Inf}.
At a split point, the evaluation follows the left-child convention used in
the method specification.
}
\examples{
fit <- list(
  tree_list = list(list(
    d = c(0L, -1L, -1L),
    l = c(0.5, -1, -1),
    theta = c(0.25, -1, -1)
  )),
  Omega = matrix(c(0, 1), nrow = 1)
)
eval_density_b(fit, matrix(c(0.25, 0.75), ncol = 1))

}
