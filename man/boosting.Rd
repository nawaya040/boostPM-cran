% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/boosting.R
\name{boosting}
\alias{boosting}
\title{Fit an Unsupervised Tree Boosting Density Model}
\usage{
boosting(
  data,
  add_noise = TRUE,
  Omega = NULL,
  ntree_max_marginal = 100,
  ntree_max_dependence = 1000,
  c0 = 0.1,
  gamma = 0.1,
  max_resol = 15,
  min_obs = 5,
  early_stop = NULL,
  alpha = 0.9,
  beta = 0,
  precision = 1,
  nbins = 8
)
}
\arguments{
\item{data}{A finite numeric matrix. Rows are observations and columns are
variables. Constant columns are not supported.}

\item{add_noise}{A single logical value. If \code{TRUE}, tied observations are
jittered using R's random-number generator before fitting.}

\item{Omega}{Either \code{NULL}, or a finite numeric matrix with one row per
variable and two columns giving lower and upper support limits. Each
training observation must be strictly inside the supplied limits.}

\item{ntree_max_marginal}{Non-negative integer. Maximum number of trees in
each marginal fitting stage.}

\item{ntree_max_dependence}{Non-negative integer. Maximum number of trees in
the dependence fitting stage.}

\item{c0}{Numeric shrinkage parameter, strictly between zero and one.}

\item{gamma}{Non-negative numeric local scale parameter.}

\item{max_resol}{Non-negative integer controlling the maximum split
resolution. Its archived interpretation permits leaves at depth
\code{max_resol + 1}.}

\item{min_obs}{Positive integer giving the minimum node observation count
used by the split rule.}

\item{early_stop}{Either \code{NULL}, or a finite numeric vector of length two.
Its first entry is the stopping threshold and its second entry is an
integer waiting-window length of at least two.}

\item{alpha}{Numeric parameter in \verb{[0, 1]} for the depth-dependent split
probability.}

\item{beta}{Non-negative numeric parameter for the depth-dependent split
probability.}

\item{precision}{Positive numeric precision parameter for the beta
distribution used in split scoring.}

\item{nbins}{Integer of at least two. Number of uniform-grid candidate split
locations.}
}
\value{
An object of class \code{boostPM_fit}. It retains the list layout of the
archived implementation and contains serialized trees, residuals, tree
diagnostics, variable importance, \code{Omega}, and elapsed fitting time.
}
\description{
Fits the unsupervised tree boosting procedure of Awaya and Ma (2024).
Marginal trees are fitted first, followed by dependence trees. The fitted
ensemble represents a probability distribution on a rectangular support.
}
\details{
If \code{Omega} is \code{NULL}, a rectangular support is constructed from the
processed data. If \code{add_noise} is \code{TRUE}, call \code{\link[=set.seed]{set.seed()}} before fitting
to reproduce the jitter and subsequent stochastic tree-fitting decisions.
Reproducibility is intended within a fixed R runtime. Exact equality across
operating systems has not been established.
}
\examples{
set.seed(1)
x <- matrix(c(0.2, 0.4, 0.6, 0.8), ncol = 1)
fit <- boosting(
  x,
  add_noise = FALSE,
  Omega = matrix(c(0, 1), nrow = 1),
  ntree_max_marginal = 0,
  ntree_max_dependence = 0
)

}
\references{
Awaya, N. and Ma, L. (2024). Unsupervised Tree Boosting for Learning
Probability Distributions. \emph{Journal of Machine Learning Research}, 25,
1--52.
}
